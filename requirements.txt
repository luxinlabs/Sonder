# Do not `pip install -r requirements.txt` directly: pipecat-ai 1.4.0 pins
# onnxruntime~=1.24.3, which has no macOS-13 wheel. Run ./setup.sh instead,
# which installs pipecat-ai --no-deps and pins onnxruntime==1.23.2 (verified
# working with Silero VAD). This file just documents the resolved versions.
# STT requires Deepgram (cloud): pipecat-ai's whisper/stt.py module
# unconditionally imports mlx_whisper on any Darwin arm64 host, and mlx
# refuses to load below macOS 13.5 -- no local-STT path exists on this
# machine (13.4.1) through pipecat. TTS defaults to Piper (local, no
# account) and only uses Cartesia if CARTESIA_API_KEY is set.
pipecat-ai[websocket,deepgram,piper,anthropic]==1.4.0
onnxruntime==1.23.2
fastapi>=0.115.6,<1
uvicorn[standard]
python-dotenv
twilio
google-auth
google-auth-oauthlib
google-api-python-client
anthropic>=0.49.0,<1
deepgram-sdk>=6.1.1,<8
piper-tts>=1.3.0,<2
requests>=2.32.5,<3
