# Do not `pip install -r requirements.txt` directly: pipecat-ai 1.4.0 pins
# onnxruntime~=1.24.3, which has no macOS-13 wheel. Run ./setup.sh instead,
# which installs pipecat-ai --no-deps and pins onnxruntime==1.23.2 (verified
# working with Silero VAD). This file just documents the resolved versions.
# STT/TTS are local (Whisper MLX + Piper) -- no cloud voice accounts needed,
# only Twilio (telephony), Anthropic (Claude), and Google (Calendar OAuth).
pipecat-ai[websocket,whisper,mlx-whisper,piper,anthropic]==1.4.0
onnxruntime==1.23.2
fastapi>=0.115.6,<1
uvicorn[standard]
python-dotenv
twilio
google-auth
google-auth-oauthlib
google-api-python-client
anthropic>=0.49.0,<1
faster-whisper~=1.2.1
mlx-whisper~=0.4.2
piper-tts>=1.3.0,<2
requests>=2.32.5,<3
